http:
  middlewares:
    guac-mwr:
        chain:
          middlewars:
            - guac-headers
            - guac-prefix
            
    guac-headers:
      headers:
        customResponseHeaders:
          Connection: "Upgrade"
          Upgrade: "websocket"
          
    guac-prefix:
      addPrefix:
        prefix: "/guacamole"
